import { DragbyDirective } from './dragby.directive';

describe('DragbyDirective', () => {
  it('should create an instance', () => {
    const directive = new DragbyDirective();
    expect(directive).toBeTruthy();
  });
});
